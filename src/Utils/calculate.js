const add = (first, second) =>{
    return first + second;
}

const multiply = (first, second)=>{
    return first * second;
}

const divideTheFirstNumberByTheScondNumber =(first, second)=>{
    return first / second;
}

export {add, multiply, divideTheFirstNumberByTheScondNumber}
// export default add;

// const calculator ={add: add}
// export default calculator;